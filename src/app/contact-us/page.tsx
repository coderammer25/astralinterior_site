import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Contact Us",
};

const ContactPage = () => {
	return (
	<div>
		
		Contact Page
		
	</div>
	)
};

export default ContactPage;
